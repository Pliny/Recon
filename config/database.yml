  development: &development
    adapter: postgresql
    database: startup-weekend-server_dev
    host: localhost
    

  # Warning: The database defined as 'test' will be erased and
  # re-generated from your development database when you run 'rake'.
  # Do not set this db to the same as development or production.
  test:
    adapter: postgresql
    database: startup-weekend_test
    host: localhost

  demo:
    adapter: postgresql
    database: startup-weekend_demo
    host: localhost

  production:
    adapter: postgresql
    database: startup-weekend_production
    host: localhost
